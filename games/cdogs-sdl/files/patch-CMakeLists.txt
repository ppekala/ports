--- CMakeLists.txt.orig	2017-10-29 01:50:36 UTC
+++ CMakeLists.txt
@@ -101,8 +101,6 @@ else()
 		if(DEBUG_PROFILE AND CMAKE_COMPILER_IS_GNUCC)
 			ADD_DEFINITIONS(-p)
 		endif()
-	else()
-		ADD_DEFINITIONS(-O2)
 	endif()
 	if(GCW0)
 		add_definitions(-D__GCWZERO__)
